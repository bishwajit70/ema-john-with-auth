/**
 * 1. Create a new project in console.firebase.google.com 
 * 2. npm install firebase
 * 3. Create a file in src called firebase.init.js and paste {initializeApp} from 'firebase/app'
 * 4. import {getAuth} from 'firebase/auth'
 * 5. export default auth;
 * 6. firebase settings > Authentication > Email / Password auth enable 
*/

/**
 * hosting setps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
*/